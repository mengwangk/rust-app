#![allow(dead_code)]

pub fn add_one(x: i32) -> i32 {
    x + 1
}

pub fn add_two(x: i32) -> i32 {
    x + 2
}

// use std::fmt;
// use std::io;
// use std::io::{self, Write};
// use std::{cmp::Ordering, io}; // std::io and std::io::write
// use std::io::Result as IoResult;
// use std::collections::*;

// fn function1() -> fmt::Result {
//     // --snip--
// }

// fn function2() -> io::Result<()> {
//     // --snip--
// }
